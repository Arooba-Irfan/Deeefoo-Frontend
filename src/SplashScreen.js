import React from 'react';
import { Dimensions,ScrollView,SafeAreaView,StatusBar,Image, StyleSheet, Text, View,ImageBackground ,KeyboardAvoidingView,TouchableOpacity} from 'react-native';

const SplashScreen= () => {
  return(
    <View style={{backgroundColor:'#C21313',width:Dimensions.get('window').width,height:Dimensions.get('window').height}}>

    </View>
    
  );
};


export default SplashScreen;